## Example of how to use the rapi client
#  This is just a placeholder for now.
# TODO Write real README file explaining project and list resources for more
#  information
import client

HOSTNAME = 'ganeti-test.osuosl.bak'

curl_config = client.GenericCurlConfig()
rapi_client = client.GanetiRapiClient(HOSTNAME, curl_config_fn=curl_config)
print rapi_client.GetVersion();
